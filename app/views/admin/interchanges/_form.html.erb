<%= form_for [:admin, @interchange] do |f| %>
  <% if @interchange.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interchange.errors.count, "error") %> prohibited this interchange from being saved:</h2>

      <ul>
      <% @interchange.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
